# Ohm's Law Assignment 1 - by Frank Addae

# Ask the user for input
I = float(input("Enter the current (in A): "))
R = float(input("Enter the resistance (in ohms): "))

# Calculate voltage
V = I * R

# Display the result
print(f"The voltage is: {V} volts")
